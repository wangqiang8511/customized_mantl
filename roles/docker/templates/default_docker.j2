DOCKER_OPTS=" -H tcp://0.0.0.0:{{ docker_tcp_port }} -H unix:///var/run/docker.sock --cluster-store=etcd://{{ private_ipv4}}:{{ etcd_client_port }} --cluster-advertise={{ private_ipv4 }}:{{ docker_advertise_port }} --graph=/data/docker -s aufs --label host={{ inventory_hostname }}"
